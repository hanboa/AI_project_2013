This is a small program to crawl Google Image Search for a number of search terms and download the first n results, resized to a given size. It uses the Java Image Sclaing library, which is present in the /lib/ folder.

The file config.txt can be edited, it has the following syntax:

NumberOfImages Width Height NumberOfThreads Keyword1 … KeywordN
